apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace : app
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      NodePort: 31000
  type: NodePort
